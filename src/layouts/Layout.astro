---
import Footer from '../components/layout/Footer.astro';
import Header from '../components/layout/Header.astro';

const { description = "Default description", title = "Default title" } = Astro.props;
---

<!DOCTYPE html>
<html lang="en" x-data="theme">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{title}</title>

    <link rel="apple-touch-icon" sizes="180x180" href="/assets/icons/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/icons/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/icons/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <!-- Alpine.js Core + Plugins (Bundled Builds) -->
    <script defer src="https://unpkg.com/alpinejs@3.13.10/dist/cdn.min.js"></script>
    <script defer src="https://unpkg.com/@alpinejs/collapse@3.13.10/dist/cdn.min.js"></script>
    <script defer src="https://unpkg.com/@alpinejs/focus@3.13.10/dist/cdn.min.js"></script>
    <script defer src="https://unpkg.com/@alpinejs/intersect@3.13.10/dist/cdn.min.js"></script>
    <script defer src="https://unpkg.com/@alpinejs/mask@3.13.10/dist/cdn.min.js"></script>
  </head>

  <body x-bind:class="{ 'dark': isDark }" class="bg-primary-50 text-primary-950 antialiased transition">
    <Header />
    <main>
      <slot />
    </main>
    <Footer />

    <style is:global>
      [x-cloak] {
        display: none !important;
      }

      :root {
        --primary-bg: #ffffff;
        --primary-text: #127780;
        --secondary-bg: #15eda3;
        --secondary-text: #0fc083;
      }

      .dark {
        --primary-bg: #0c4d53;
        --primary-text: #ffffff;
        --secondary-bg: #2cffb7;
        --secondary-text: #30c6d3;
      }

      body {
        background-color: var(--primary-bg);
        color: var(--primary-text);
        font-family: 'Radnika Next', sans-serif;
      }

      header, footer {
        background-color: var(--secondary-bg);
        color: var(--secondary-text);
      }
    </style>

    <!-- View Transition Polyfill or Feature-Detect -->
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        if ('startViewTransition' in document) {
          document.startViewTransition(() => {
            console.log('ViewTransition started.');
          });
        } else {
          console.warn("ViewTransition API not supported.");
        }
      });
    </script>

    <!-- Alpine Initialization Confirmation -->
    <script>
      document.addEventListener("alpine:init", () => {
        console.log("✅ Alpine.js loaded and initialized.");
      });
    </script>
  </body>
</html>
